import { McpDetails, SupportedAgents } from './consts/agents';

export function readMCPConfiguration(agent: SupportedAgents, options?: {}) {}

export function writeMCPConfiguration(configs: McpDetails[], agents: SupportedAgents[], options?: {}) {}
